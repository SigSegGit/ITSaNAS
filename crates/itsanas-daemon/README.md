# itsanas-daemon

Background service tying storage, net, repair, and quota together; exposes
the authenticated API used by the CLI and Android clients (D9). Placeholder
until M1+ — see `ARCHITECTURE.md` at the repo root.
