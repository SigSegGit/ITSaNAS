//! Command-line client: talks to a local or remote `itsanas-daemon` over its
//! authenticated API.
//!
//! Placeholder crate: no implementation yet. Real work starts once
//! `itsanas-daemon` exposes an API to talk to (M1+). Will gain a `main.rs`
//! binary entry point at that point.
